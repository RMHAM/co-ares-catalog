export interface Channel {
  order: number;
  config: string;
  name: string;
  users: string;
  rxFrequency: string;
  rxWidth: string;
  rxTone: string;
  txFrequency: string;
  txWidth: string;
  txTone: string;
  mode: string;
  remarks: string;
}
