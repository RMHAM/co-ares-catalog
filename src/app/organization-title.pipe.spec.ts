import { OrganizationTitlePipe } from './organization-title.pipe';

describe('OrganizationTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizationTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
